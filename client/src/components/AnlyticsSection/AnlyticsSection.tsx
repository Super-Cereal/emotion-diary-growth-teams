import b_ from 'b_';
import { Hexagon } from '../Hexagon/Hexagon';

import './AnlyticsSection.scss';

export const b = b_.with('analytics-section');

export const AnlyticsSection = () => {
    return (
        <section>
            <Hexagon />
        </section>
    );
};
